# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# BMAD / Claude 工具链（开发环境依赖，非项目源码）
.claude/

# npm lockfile（repo 用 bun.lock，禁止 npm 残留）
package-lock.json

# BMAD 框架内核（按需，当前不提交）
_bmad/

# Playwright MCP 工具产物 / 仿真截图（临时产物，不提交）
.playwright-mcp/
/sd-sim-*.png

# Playwright e2e output (Story 1a.3 C3 fold)
playwright-report/
test-results/
~/.cache/ms-playwright/

# Go / Rust / Wasm 构建产物（Story 1a.1 三语言基座）
/target
/wasm/target
/wasm/pkg
*.tsbuildinfo

# 测试覆盖率
/coverage

# SQLite 数据库（Story 2.1: users/sessions 表，含用户邮箱与 session token，不提交）
/data

# Go build 产物（本地编译二进制，不提交）
/newsd.exe
