---
topic: NewSD 赛博朋克 ASCII 系统动力学多人协作建模平台 MVP
updated: 2026-07-02T21:00
---

- (decision) spec 语言 = 中文正文 + 英文术语/ID(AD-n/FR-n/CAP-n/字段名), 与 PRD/spine 既成风格对齐. config.yaml communication_language/document_output_language 由 English 改 Chinese 根因修复(安装器默认未改).
- (decision) spine 作为 adopted companion(非 source): 下游 bmad-spec/epics-and-stories 仍须读 spine 的 AD-1..15 + 图 + Capability Map, 故不入 sources(已吸收) 而入 companions. PRD 与 addendum 完全吸收进 kernel + companions, 入 sources 仅供 audit.
- (decision) capability 映射: PRD 的 FR 按'用户能做什么'蒸馏为 CAP-n(intent+success 双字段). 19 个 FR 不 1:1 转译为 19 CAP(那会退化为 FR 复读), 按产品能力域归并: 画布/图元/求解/协作/多画板剪贴板/撤销/界面/游戏化. FR ID 在 success/约束里作为可测证据保留(Spec Law rule 5).
- (decision) constraints 直接采用 spine 的 15 条 AD 作架构不变量(AD 是已裁定的 design-bender), kernel 引述 AD-n Rule 要点 + 指向 spine companion 详文. 7 大数学约束作产品级约束单列(来自 PRD §2.2, 非 AD, 是 SD 域不变量).
- (decision) open_questions 接纳 spine Deferred 的 7 条(实现期标定类: F5-perf/F1-quality/F6-threshold/F7-snapshot-freq/F15-op-quota/FR-SIM-8-convergence-params + prettier baseline) + PRD 开放问题 2/3(最大画板尺寸 / 匿名 vs 认证). 实现期标定类非 spec 阻断项, 但须显式列为 open question 不沉默.
- (event) spec create 自验证 Pass 1(Coherence)通过: Spec Law 1-6,8 全过. 12 CAP 皆有 intent+success(rule1); intent 描述 WHAT, HOW 落 companion(rule2); constraints 逐条 AD 带 Prevents 真排他(rule3); non-goals 12 项含被拒方案(rule4); success signal 含 FPS/延迟/反指标归零 + 可演示瞬间(rule5); CAP-1..12 ID 稳定唯一(rule6); prose 精简(rule8).
- (event) spec create 自验证 Pass 2(Preservation)通过: AD-1..15 全部入 kernel constraints(逐条 cite); PRD 36 FR 全部 load-bearing 内容落入 CAP intent/success 或 companion(spine Consistency Conventions/math-constraints.md). 编辑后补 cite FR-SIM-1/FR-COLLAB-1/FR-COLLAB-2/FR-ELEM-1/FR-ELEM-2/FR-ELEM-4/FR-SIM-VIZ-1 增强 audit 可溯. Wrapper-only 内容(PRD §1.3 用户旅程脚注/§七 架构示意框图/§八 术语表重复定义)未落入——属包装仪式,故弃,已记录.
- (event) spec finalized: SPEC-NewSD 产出 SPEC.md(5-field kernel: Why/Capabilities×12/Constraints/Non-goals×12/Success signal + Assumptions×5 + Open Questions×10) + 3 spec-authored companions(architecture-diagrams.md/math-constraints.md/stack.md) + 1 adopted companion(spine 相对路径引用) + sources(PRD+addendum 完全吸收). memlog 7 entries(init+5 decision+2 event verdict). bmad-spec 以 spine 为 companion 落地,下游 epics-and-stories 可启动.
- (decision) 假设3 粘贴原子性 = 选项 B(允许连线,撤销级联删除). 改 spec CAP-8/CAP-9: 粘贴为单 CRDT 插入事务但允许连线,撤销级联删除粘贴图元+其后所连流量,级联边界=粘贴事务图元+同一撤销窗口内连出流量. 原 A 硬阻断假设作废. 来源: 用户决策.
- (decision) F1-quality 验收口径 = 锁严格(目视不可区分). spec CAP-11 第9/10项 + Open Questions 收口: 图集辉光须与 shadowBlur 目视无差异, 实现期验证达不到则按 FR-UI-6 不达标处理不得回退 shadowBlur(违 AD-9). 严于 spine Deferred 待定, 建议回写 spine F1-quality 同步(offer). 来源: 用户决策.
- (question) MVP 认证模式: 用户初选引入轻量认证, 但与现有架构硬冲突(addendum §3.2 鉴权非 MVP 阈值触发 / PRD §4.3 匿名无 Cookie / spine 无鉴权 AD / SQLite 无用户表 / FR-GAME-2 现写匿名持久化策略见开放问题3). 执行路径待确认: CC 已 surface 冲突, 三选项待用户定(a 真认证需 spine 回 draft 增 AD-16+修订 addendum §3.2+PRD §4.3 / b 轻量持久化标识 localStorage 匿名ID绑徽章无鉴权不违前提 / c 维持匿名原推荐). 未决前不 finalize CAP-6/CAP-12 认证相关措辞.
- (decision) Q2 认证模式最终决策 = 真认证系统(场景B跨设备徽章跟随+场景C只读权限都要). 波及面: (1) spine 回 draft 增 AD-16 鉴权模型 + AD-17 CRDT权限模型(owner/editor/viewer); (2) 修订 addendum §3.2 鉴权'非MVP阈值触发'→'MVP引入'; (3) 修订 PRD §4.3 'MVP匿名无Cookie无CSRF'→认证会话; (4) SQLite schema 加用户表+会话表(architecture-diagrams ERD 同步); (5) Go WS gateway 握手鉴权; (6) FR-GAME-2 重定 徽章持久化从匿名localStorage→绑账号; (7) 部署包络图+依赖方向图更新. 推翻已finalized匿名前提(status:final 双证作废须重跑), 重开架构裁定走PR. 来源: 用户经4场景对比后选真认证.
- (decision) Q2 auth 机制细化(波及 AD-16/17/18 → CAP-13 + Constraints + Non-goals):OAuth 直连 GitHub+Google 双 provider 共享 users 表 UNIQUE(oauth_provider, oauth_user_id);session token 非 JWT 双通道下发——HttpOnly Secure Cookie + JSON body(解 JS 不可读 cookie 致 WS 首帧 token 矛盾);WS 握手首帧 token 鉴权(token 仅存 JS 内存不可跨域读取致浏览器端 WS CSRF 不可行,Origin 作 defense-in-depth);owner/editor/viewer 三角色 viewer CRDT op 网关拒收;owner 转让/角色修改走认证 HTTP 端点非 CRDT(防提权);权限变更经 role_change WS frame 传播+drain 原子性;share_token crypto/rand ≥128 bits+可轮换+Referrer-Policy no-referrer;CRDT 持久化表按 board_id 分区;徽章触发服务端从 op 流判定防客户端伪造;[SYSTEM HALTED] 熔断经 WS 上报服务端写 stdout;部署 = 单节点云托管 Dockerfile+CI/CD,SQLite 持久卷(平台不支持则提前触发 §3.2 PG 迁移),垂直扩容优先。来源: spine AD-16/17/18 re-finalize + Reviewer Gate
- (event) Q2 收口:PRD 开放问题 3(MVP 仅匿名协作还是需要认证系统)闭合为决策——MVP 引入 OAuth 认证(GitHub+Google)+ 画板权限模型(owner/editor/viewer)。spec 重 derive:Non-goals 删 auth 行,加 CAP-13 OAuth 认证与画板权限,Constraints 补 AD-16/17/18 + AD-2 演进为云托管,开放问题 3 改为已闭合,F15-op-quota 措辞 匿名→认证后 editor,新增 3 个 Reviewer Gate defer-with-revisit 开放问题(服务端重启恢复/房主迁移触发协议/SQLite 备份零停机)。来源: bmad-spec update re-derive
- (event) 2-pass self-validate PASS。Pass1 Coherence:CAP-1..13 全 intent+success,AD-16/17/18 constraints 防止子句齐,CAP ID 稳定唯一,lean。Pass2 Preservation:AD-1..18 全在 kernel(intro 18 条 AD + AD-1..AD-18 引用),36 FR 保留,Q1(paste→B)+Q3(F1-quality 严格)上轮收口不变,Q2(auth)→CAP-13+AD-16/17/18+开放问题3闭合。匿名→认证 传播完成:Non-goals/F15 已改,残留仅 AD-16 防止子句(防匿名前提致徽章丢失)+ memlog 不可变历史。spec 重 derive 闭环
- (decision) 假设3 粘贴原子性 改定方案 C(supersede entry 14 选项 B)。/audit 检出 Type_B 状态机不一致:Assumptions §3 取选项 A 硬阻断 vs CAP-8/CAP-9 取选项 B 级联删除组。方案 C:粘贴为单个 CRDT 原子事务(FR-BOARD-2),撤销粘贴 = 删粘贴图元 + 内部重映射流量,外部流量及公式引用按统一删除 RI 规则处理,无跨事务 undo 归并(与 FR-HISTORY-1 用户操作级粒度一致)。统一删除 RI 规则(C2):endpoint RI(删存量→级联删以其为 fromId/toId 端点的流量,防悬空流量)+ formula-reference RI(流量公式 @uuid 引用被删存量但非端点→AST ref 节点标 dangling 红色高亮 + 状态栏告警,不删流量,与 CAP-2 重命名不断引用友好取向一致)。原选项 A 硬阻断 block 在 CRDT 原子事务瞬时提交下窗口=0(死文本);原选项 B 级联删除组须跨 undo 事务归并违 FR-HISTORY-1 粒度。波及:SPEC.md CAP-8/CAP-9 success + Assumptions §3(三处)+ Constraints 新增统一删除 RI 规则条 + prd.md 假设3(line 579 改决策方案 C + 脚注)。来源:/audit Type_B + 用户决策(决策1 C2 公式引用 dangling 不删流量 / 决策2 同步改 prd)。
- (event) F1-quality offer 已回写 spine(supersede entry 15 offer-pending)。spine Deferred F1-quality 行(388)+ VRAM glow quality acceptance 行(395)同步锁 visually indistinguishable(严于 prior open 状态),offer 标记从"建议回写"→"已同步"。spec CAP-11 第9/10项验收口径目视不可区分不变(本就严于 spine Deferred,现 spine 对齐)。来源:用户确认 offer 接受。
- (decision) PresenceSnapshot 勘误(spine + spec ERD companion)。/audit 检出第二处硬矛盾:spine 内部不一致(依赖图行 159 In-memory Presence vs ERD 行 291 SQLite persists);spec ERD companion 经"便利复现"继承。根因查证 prd/.memlog.md:8-9 原始决策 C2 = "服务端 SQLite + 内存 Presence 不跨会话持久化"(FR-COLLAB-5),spine ERD 把 PresenceSnapshot 放 SQLite 是偏差方非 PRD 原意。勘误:PresenceSnapshot 从 SQLite 持久化集移除,改进程内存态按 board_id 键(会话结束丢弃,FR-COLLAB-5)。波及 spine:ERD(移 SQLite persists 行 + Board 关系改 in-memory keyed + entity 标 not persisted)+ AD-17 Rule(移 PresenceSnapshot 出 CRDT 持久化表列表,留 CRDTSnapshot/OpLog)+ Consistency 表(同);波及 spec:architecture-diagrams.md ERD(同 3 处)+ epics.md:189 schema 列表(PresenceSnapshot 标进程内存态)。此为 errata 级(ERD + AD-17 措辞),不触发架构 re-review(spine status:final,re-finalize 有先例 PR#7 AD-16/17/18)。来源:/audit Type_B + 用户"并入"决策。
