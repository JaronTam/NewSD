---
scope: NewSD 赛博朋克 ASCII 系统动力学多人协作建模平台 MVP 架构
purpose: 收口 handoff 7 条 open-high(#1-#7) + #8/#9/#10 + FR-CANVAS-5/FR-UI-6;裁定范式/边界/状态所有权/降级接口
altitude: initiative->feature (PRD->架构, 整个系统)
updated: 2026-07-02T00:00
---

- (decision) F2 求解器范式 = Wasm 硬承诺. MVP 即上 Rust mexpr + Wasm 求解器内核(隐式 BDF+牛顿+autodiff+稀疏 LU+量纲校验+非负钳制+DELAY 展开全在 Wasm 内). #7 熔断 = Wasm.Memory 64MB 硬隔离 + 单步 wall-clock <=500ms + 牛顿 maxIterations 100 + AST MAX_NODES 5000, 超限 ASCII 通知 [SYSTEM HALTED: ...]. TS 侧仅薄 Solver 边界调用 Wasm exports. Binds: FR-SIM-1/4/6/7/8, #1/#2/#7, FR-COLLAB-4. Prevents: TS 主线程数值爆炸污染 UI 帧率 / 恶意公式无沙箱逃逸. Rule: 所有仿真步数值求值经 Wasm 内核, TS 不得直接跑仿真步. prototype 现有 src/lib/sd/formula.ts evalFormula(纯 TS 递归下降) 仅留作非仿真场景(UI 实时预览/量纲预览)待裁定, 仿真路径须替换.
- (decision) F3 后端 = Go 单体. PRD §7 'Go / Node.js' 裁定为 Go. 后端职责: WebSocket 接入网关 + CRDT 中继 + SQLite 持久化(CRDT 快照+操作日志) + 进程内存 Presence/仿真态快照(FR-COLLAB-2/5). 仿真在房主客户端(FR-COLLAB-3), 服务端不跑仿真. 选 Go 理由(抛开最省): 静态类型 + goroutine 契合 WebSocket 并发 + 单二进制部署 + 后端长生命周期服务演进稳健. 代价: yjs Go 服务端生态弱, 须裁定实现路径(下条 open question). Binds: FR-COLLAB-1/2/3/5, FR-BOARD-1. Prevents: 双语言栈跨服务 IPC 复杂度(混合方案) / Node 弱类型后端演进. Rule: 后端单 Go 进程, 不引入 Node 服务.
- (question) F3-yjs-server: Go 侧 yjs CRDT 中继实现路径待裁定. 选项: (i) y-crdt Go binding + 自研 WebSocket 中继(greenfield 重活, 与 addendum §7.3/§8.3 体量警示同向) (ii) 换 Go 原生 CRDT 库(automerge-go 不成熟, 风险) (iii) 仍 Node 跑 yjs shim + Go 跑其余(退回混合, 违 F3 单 Go 决策). 倾向 (i), 但须确认 y-crdt Go binding 成熟度 + 自研中继工作量. 此项为架构阶段须收口的 open question, 不阻断其余 fork.
- (decision) F4 #1 BDF 起步策略 = 方案一. BDF-1~2(不上 BDF-3+). 起步: 第一步后向欧拉(BDF-1)单步自洽, 第二步起升阶 BDF-2; 起步失败回退显式试探步(接 FR-SIM-8 降级链). 步长控制: 固定 dt 默认 0.1(档位 0.01/0.1/0.5/1.0, FR-UI-1), 非 MVP 自适应(自适应留 FR-SIM-8 降级链内部). 牛顿初始猜测按阶数分配: BDF-1 恒值外推(y_{n+1}=y_n), BDF-2 线性外推(2y_n - y_{n-1}). 线性外推刚性瞬态发散概率与 FR-SIM-8 收敛监控阈值联调标定(残差范数/步长收缩率), 回退触发后降级链闭环. Binds: FR-SIM-1/8, #1. Prevents: BDF-2+恒值外推退化一阶违§1.1无感(频繁降级黄点) / BDF-3+过度工程稳定降 / 纯后向欧拉降级链首级空转. Rule: Wasm 求解器内核按阶数切换状态机起步, 牛顿初始猜测按当前阶数分配外推法.
- (decision) F5 #2 雅可比激活更新策略 = 方案A. 约束激活/释放后雅可比全重算(autodiff 重算整个 J, 不用 Broyden 近似——MVP 100 存量规模 Broyden 收益边际小漂移风险不对称). 防 Chattering = hysteresis band(激活阈值 stock<0 / 释放阈值 stock>ε, ε 与模型量级联调标定). 活动集法(active-set)优先: 显式维护当前激活约束集, 激活约束当等式(stock=0)求解, 与 FR-SIM-4 速率级钳制语义一致(钳制流出速率使 stock 恰好归零); 投影法兜底: 活动集法失败时投影到可行域. Binds: FR-SIM-4, #2. Prevents: Broyden 累积漂移致长仿真后期牛顿发散难排查 / Chattering 致雅可比频繁重算性能崩 / 后置钳制(nv=0)违 FR-SIM-3 物质守恒幽灵渗漏. Rule: Wasm 求解器内核维护活动集状态机 + 滞回带, 约束激活后全重算雅可比与 LU 稀疏模式.
- (question) F5-perf: 全重算雅可比+稀疏 LU 在 100 存量下能否达 PRD §1.4 的 100 步/s, 实测前不可定. 若不达, Broyden 近似须重评. 此为实现期标定 open question, 非架构阶段可决, 不阻断采纳方案A.
- (decision) F1 渲染裁决 #8 = (a) VRAM 路径. 采纳 PRD §2.1 既定方向, 不改 PRD §1.4/§4.1 指标. 实现重点: 辉光图集(glow atlas, 离屏预渲染每 ASCII 字符×亮度档带辉光到纹理) + VRAM 双缓冲(文本缓冲存字符码 + 颜色缓冲存颜色索引, 逐帧仅更新脏区域联动 FR-CANVAS-4) + 色相偏移 Shader(GPU 片段着色器做色相旋转复刻 shiftHue, nearest 采样保像素风, 禁插值). 禁用 per-glyph shadowBlur(prototype glowFor 2-11px 逐字符 shadowBlur 弃用). FR-UI-6 #9 CRT背景漂移 + #10 per-glyph 辉光 用本方案复刻(图集预渲染时固化亮度绑定). FR-CANVAS-5 小地图复用图集降采样率独立脏矩形. 用户选 (a) 因用户体验最高, 接受质感复刻未验证风险. Binds: FR-CANVAS-3/4/5, FR-UI-6, PRD §2.1/§1.4/§4.1, #8. Prevents: shadowBlur 逐字符 GPU 模糊致 1000 图元@60FPS 不可达 / 改 PRD 违§1.1 硬约束门槛 KPI. Rule: 渲染层用图集预渲染 + 双缓冲 + Shader, 禁 per-glyph shadowBlur.
- (question) F1-quality: VRAM 图集辉光能否复刻 prototype per-glyph shadowBlur 的霓虹质感, 架构期无法实测. 须实现期跑质感原型: 图集辉光 vs shadowBlur 目视对比, 定验收口径(目视不可区分 vs 风格保留可接受差异). 此为 open question, 不阻断架构产物(下游 FR-UI-6 #9/#10 + FR-CANVAS-5 预算按 (a) 方案落 AD, 实现期验证若失败须回评 (b)/(c)).
- (decision) F6 #3 AST 结构性冲突回退 = 方案C 分级回退(安全最高). 轻冲突(场景A 括号改结合性 / 场景B 运算符替换局部): 标冲突区 + 锁定相关子树隔离错误不阻断其余区域. 重冲突(场景C 删除子树幽灵编辑): 回退文本级(CRDT 降为 Y.Text 文本合并), 保底不语义错. 合并后对 AST 做拓扑比较检测结构性冲突. 分级阈值判定原则(非穷举): '冲突是否改变运算符语义或产生幽灵引用' 判为重, 余为轻. 重冲突回退文本级明文标'罕见兜底非常态降级', 与 ⑤ 决策(C 语义合并)不矛盾(兜底非默认). Binds: FR-COLLAB-6, #3, #4(待定节点冲突作子集). Prevents: 单一策略场景C 覆盖硬伤(锁子树边界难定 / 标冲突区错误传播). Rule: AST 合并后拓扑比较检测, 分级回退轻标区+锁子树重文本级.
- (question) F6-threshold: 分级阈值(轻/重冲突判定)须与 #4 待定节点合并规则联调, 架构期无法穷举冲突场景. 定判定原则'运算符语义改变或幽灵引用=重'非枚举. 此为 open question, 实现期枚举真实冲突场景校准.
- (decision) F7 #5 快照-CRDT 时序对齐 = 采纳 handoff #5. 快照附 CRDT 文档版本标识(Y.Doc clientID+clock 向量). 新房主拉快照校验: 对齐(快照版本=当前 CRDT 模型版本)→直接续跑; 不对齐(模型已变)→增量重跑(用当前 CRDT 模型从快照 simulationTime 赶至现在). 不阻塞 CRDT 变更(handoff #5 明文排除阻塞协作). 保反指标'房主迁移仿真状态清零=0'(不回退 t=0). Binds: FR-COLLAB-3/5, #5, PRD §1.1 反指标. Prevents: 新房主旧模型结构+新初始值续跑跳跃不连续 / 阻塞 CRDT 损协作. Rule: 快照须带 CRDT 版本向量, 新房主校验对齐决定续跑/增量重跑.
- (question) F7-snapshot-freq: FR-COLLAB-5 '周期性上报'未给具体频率. 频率低→CRDT 版本落后多→增量重跑追赶成本高(新房主启动延迟); 频率高→服务端内存+房主上报开销. 须定快照上报频率平衡追赶成本. 架构期给建议值(如每 N 仿真步或每 M 秒), 实现期实测校准. open question.
- (decision) F8 #6 降级接口 ⑤→A 三项前提 = MVP 预留. 预留三项抽象前提(定义接口不实现降级逻辑): (1) 数据兼容层 CRDT 快照读取兼容两种 formula 格式(Y.Map 嵌套 AST vs 扁平 string); (2) 编辑器抽象 AST 感知编辑器可替换为普通文本编辑器; (3) 校验管道抽象 量纲校验与 autodiff 不硬依赖 CRDT AST, 接受'任意来源 AST'. 预留质量约束: 抽象须隔离降级两端差异非空接口(防抽象泄漏致降级仍重构). 保 ⑤ 降级(结构化 AST→扁平字符串)可触发, 逃生阀首级不失效. Binds: #6, §5.1 逃生阀, addendum §8.3. Prevents: ⑤ 降级名存实亡(handoff #6 明文) / 体量超载时重构多模块成本非线性. Rule: MVP 须定义三项抽象接口隔离降级两端, 降级逻辑本身可不实现.
- (version) yjs-go (averyyan/yjs-go) verified via Context7: Go Yjs CRDT impl, supports Y.Map/Y.Array/Y.Text + sync protocol (SyncStep1/2/Update) + ApplyUpdateV2 + gorilla WebSocket. Reputation Medium, 17 snippets. F3 Go单体服务端 CRDT 中继可行,成熟度低于 JS y-websocket(High rep)。F3-yjs-server 倾向 resolved: 选 yjs-go,带成熟度 caveat。
- (version) mexpr crate does NOT exist on crates.io (API confirmed: 'crate mexpr does not exist'). addendum §1.2 绑定的 mexpr 不存在 — F2 前提部分失效。真实候选: meval v0.2.0(2018-09-30 stale, parser+eval 无 autodiff) / autodiff v0.7.0(2023-03-29, 纯 autodiff 非parser) / faer v0.24.4(2026-06-24 current, 线性代数 LU/sparse) / symbolic_expressions v5.0.3(2017 stale)。无单一 crate 同时做 parse+autodiff+LA。
- (question) F2-amend (mexpr 不存在后的求解器 crate 组合): (i) 组合 meval(parser,stale)+autodiff+faer(LU) — 三依赖,meval 8年未更新风险; (ii) 手写递归下降 parser(复用 prototype formula.ts 思路,扩 @uuid+[单位])+autodiff crate+faer — parser 自主可控,autodiff 仍借库; (iii) 全手写 parser+dual-number autodiff+faer — 最少依赖,工作量最大。revisit: 选定后须在求解器专项规约中标定 sparse LU 是否用 faer。
- (decision) F2-amend resolved (mexpr 不存在后求解器 crate 组合): 选 (ii) 手写递归下降 parser + autodiff crate + faer。Binds: parser 复用 prototype formula.ts 结构扩 @uuid/[单位] 产生式(#9/#10 直落);AST 为单一真相源供 autodiff 图/量纲校验/tokenizer 共用;LU/sparse 交 faer(v0.24.4 current)。Prevents: meval 不暴露 AST 致双源翻译层、meval 8年 stale、全手写 AD 数值正确性自担。Rule: parser 手写、autodiff 借 autodiff crate(0.7.0)、LU 借 faer;不引入 meval。amend 原则 F2(mexpr+Wasm)中 mexpr 部分作废,Wasm 边界保留。
- (decision) F-Gap1-A resolved (Fork ① pending.rawText): 选 Y.Text 插入合并。Binds: 待定节点 rawText 字段为 Y.Text,并发输入插入合并不丢字。Prevents: string 键级 LWW 并发输入一边丢。Rule: pending 节点 {type:'pending', rawText:Y.Text, cursorHint:number|null}。
- (decision) F-Gap1-B resolved (Fork ② 括号显式存): 选 O1 不显式存 group + 并行 paren 副本。Binds: AST 节点纯语义(binop/unop/ref/num/pending),括号不进 AST;公式根挂 parens 旁路(Y.Array of maps),按 nid(UUIDv4)引用 AST 节点;每个 AST 节点带 nid 字段。E1 结合性冲突归因靠 paren 副本命名,E2 冗余括号靠 parser 吸收+副本去重。Prevents: 显式 group 进 AST 致啰嗦+去冗余 pass 静默改写用户输入+g-4 链式判定非局部;不显式存纯版致结合性冲突归因模糊。Rule: paren 副本是 AST 外双结构(轻旁路),非 AST 内 group 节点;op-4 结合性翻转时 paren 不自动迁移(子树重建语义正确,用户重加);渲染层须投射 paren 回括号。对称性依据: 双结构不可避免,O2 单结构在 g-4 下退化回双结构,O1 是双结构最自然形态。
- (decision) F-Paradigm [ADOPTED] NewSD 顶层设计范式四要素: (1) Host-Authoritative Simulation 房主权威仿真——仿真在房主客户端单点计算(FR-COLLAB-3/4),服务端不跑仿真仅中继;新房主迁移续跑带 CRDT 版本校验(见 F7)。 (2) CRDT 协作文档模型——画板与公式 AST 存 Yjs CRDT(Y.Map 嵌套 AST + paren 副本,见 F-Gap1-B),Y.Text 待定节点(见 F-Gap1-A),服务端 Go 单体中继(见 F3)。 (3) Wasm 数值内核——所有仿真步数值求值经 Rust+Wasm 内核(隐式 BDF+牛顿+autodiff+稀疏 LU+量纲+非负钳制),TS 侧仅薄边界调用(见 F2/F2-amend)。 (4) Canvas 2D 定点渲染——VRAM 双缓冲+辉光图集+色相偏移 Shader,禁 per-glyph shadowBlur(见 F1)。 Binds: 全部 FR-SIM/FR-COLLAB/FR-CANVAS/FR-UI, PRD §1.1/§1.2/§2.1。 Prevents: 各子系统范式漂移(如仿真散到多客户端 / 渲染回退 shadowBlur / 数值跑 TS 主线程)。 Rule: 四范式为架构顶层不变量,子系统 AD 不得违背。
- (decision) F-Envelope [ADOPTED] 运行包络/部署: MVP 单节点部署。客户端 = 浏览器加载 Wasm+React 前端(TanStack Start + Vite 构建,沿用 prototype 栈);服务端 = Go 单二进制进程(WebSocket 接入 + CRDT 中继 + SQLite 持久化 + 进程内存 Presence/仿真态快照,见 F3)。SQLite 单文件(WAL 模式),服务进程内访问,无独立 DB 进程。无容器化要求 MVP(单二进制 + 单 SQLite 文件直部署)。环境: 客户端现代浏览器(须支持 WebAssembly.Memory 64MB 上限 + WebGL 片段着色器,见 F1/F2 #7);服务端 Linux 单机。infra/provider: MVP 不绑云厂商,单机部署即可;后续水平迁移触发条件见 addendum §3.2(PostgreSQL/Redis/多节点 WS 网关,可观测阈值触发)。operations: FR-SIM-6/#7 熔断以 ASCII [SYSTEM HALTED] 反馈;运维监控 MVP 不上独立可观测栈,留 addendum §3.2 阈值实测后引入。 Binds: F3, F2(#7 Wasm.Memory), F1(WebGL), addendum §3.1/§3.2。 Prevents: 运行包络整维度空缺(skill Finalize 失败定义)/ MVP 过度工程化上容器+云+可观测栈。 Rule: MVP 单节点单二进制 + SQLite WAL,无容器/云/独立可观测栈,迁移走 addendum §3.2 阈值。
- (decision) F3-yjs-server resolved: 选 yjs-go(averyyan/yjs-go)作 Go 侧 Yjs CRDT 中继实现。web-verify(Context7): 支持 Y.Map/Y.Array/Y.Text + sync protocol(SyncStep1/2/Update)+ ApplyUpdateV2 + gorilla WebSocket。Binds: F3 Go 单体后端 CRDT 中继路径落地。Prevents: 自研 Go Yjs binding 重活 / 换不成熟 automerge-go / 退回 Node 混合违 F3。Caveat: reputation Medium、17 snippets,成熟度低于 JS y-websocket(High rep),生态较薄。回退路径: 若 yjs-go 在服务端 CRDT 中继场景遇阻(如 sync protocol 边界 bug/性能不达),回评自研 Go Yjs 中继(基于 y-crdt FFI 或 yjs-go fork 补全),不退回 Node 混合(保 F3 单 Go)。此 caveat 入协作规约,实现期首验 yjs-go sync protocol 正确性。Rule: Go 服务端 CRDT 中继用 yjs-go,遇阻回评自研非退 Node。
- (decision) F-NonHostDataflow (Reviewer Gate hole #1): 非房主客户端权威仿真态来自房主计算结果经服务端中继广播, 非本地计算仿真步. 非房主客户端可跑本地非权威预览(量纲/视觉, 非仿真步), 但权威仿真显示订阅房主广播. Binds: AD-1(房主权威仿真), FR-COLLAB-3, AD-11(快照续跑). Prevents: 非房主客户端本地跑 Wasm 仿真步致与房主结果分叉/每次按键闪烁. Rule: 非房主客户端仿真显示态由房主广播驱动(订阅非计算), 本地 Wasm 仅限非仿真预览. 来源: Reviewer Gate adversarial lens hole #1, F-Paradigm 房主权威支柱补全.
- (decision) F-BreakerBoundary (Reviewer Gate hole #5): 熔断(#7/AD-5 资源耗尽)与降级(FR-SIM-8 收敛压力)边界裁定: 资源耗尽类(Wasm.Memory 64MB / 单步 wall-clock >500ms / AST MAX_NODES 5000 编译期)与迭代预算类(牛顿 maxIterations 100 仍不收敛)走熔断 [SYSTEM HALTED] 暂停报错; 残差范数不收敛(数值收敛压力)走 FR-SIM-8 降级链(降级不阻断). 两者不混淆触发. Binds: AD-5, FR-SIM-6/8, #7. Prevents: 同一牛顿不收敛被两 builder 分别路由到熔断(暂停)或降级(续跑)致相反结果. Rule: 资源/迭代预算超限→熔断 halt; 残差非收敛→FR-SIM-8 降级. 来源: handoff §二.4 + Reviewer Gate hole #5.
- (question) F15-op-quota (Triage): 匿名协作 CRDT ops/stockId 伪造防御(#15, R3 medium, handoff §三). 准入校验 + op/s 配额 + Awareness 限制 + 房间容量硬限制 + stockId 存在性验证. 此为安全防御专项收口项, 架构阶段不阻断 spine, defer 到安全规约细化. revisit: 实现期协作服务端上线前定配额阈值与 stockId 校验落点; SQLite 参数化查询显式要求一并落.
- (question) FR-SIM-8-convergence-params (Triage): 求解器自适应降级收敛监控阈值(handoff §二). 残差范数 tol(与 dt/模型量级联调) + 步长收缩率 N/ρ(连续 N 步缩减因子<ρ 视崩溃) + BDF→后向欧拉/后向欧拉→显式试探步切换条件 + 显式试探步失败→二级单按钮预设降级组合. 架构阶段不定具体数值(无法穷举), defer 到求解器专项规约标定. revisit: 实现期与 #1 步长控制 + #2 雅可比更新策略联调标定; 与 AD-5 熔断边界(资源/迭代预算→halt)区分(残差非收敛→降级不阻断).
- (event) spine finalized. ARCHITECTURE-SPINE.md frontmatter status: draft→final, updated 2026-07-02. 15 ADs (AD-1..AD-15) monotonic, lint_spine.py ok:true 0 findings. Finalize 7 步闭合: (1) Distill 22→26 memlog entries; (2) Reconcile 修 2 PRD gap (FR-SIM-2 入 AD-5 Binds + Capability Map 补 FR-SIM-2/3/5); (3) Reviewer Gate lint clean + 2 lens (web-verify/adversarial), 5 hole 4 修 (AD-15 新增 + AD-5/AD-12/AD-14 收紧) 1 defer (snapshot quiescence, 已被 version vector 覆盖); (4) Triage 7 open Q 全 defer 带 revisit (F5-perf/F1-quality/F6-threshold/F7-snapshot-freq 原条目内含 revisit + F15-op-quota/FR-SIM-8-convergence-params 新增); (5) Render lovable-deck-prompt.md (双层受众, deck 产品留 Lovable 侧); (6) External handoff: addendum §1.2 mexpr 旧表述修订并入架构 PR (对应 AD-6), bmad-spec 以 spine 为 companion (AD ID stable); (7) Close 本条 event + frontmatter status:final 双证. 来源: bmad-architecture Finalize.
